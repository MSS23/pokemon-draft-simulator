import { LoadingScreen } from '@/components/ui/loading-states'

export default function DraftHistoryLoading() {
  return <LoadingScreen title="Loading Draft History..." description="Fetching past drafts." />
}
