import { LoadingScreen } from '@/components/ui/loading-states'

export default function DashboardLoading() {
  return <LoadingScreen title="Loading Dashboard..." description="Fetching your drafts and activity." />
}
