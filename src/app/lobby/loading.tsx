import { LoadingScreen } from '@/components/ui/loading-states'

export default function LobbyLoading() {
  return <LoadingScreen title="Loading lobby..." description="Finding active drafts and leagues." />
}
