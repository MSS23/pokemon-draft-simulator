import { LoadingScreen } from '@/components/ui/loading-states'

export default function CreateDraftLoading() {
  return <LoadingScreen title="Setting Up Draft..." description="Preparing draft configuration." />
}
