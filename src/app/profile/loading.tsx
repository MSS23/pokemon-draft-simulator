import { LoadingScreen } from '@/components/ui/loading-states'

export default function ProfileLoading() {
  return <LoadingScreen title="Loading Profile..." description="Fetching your trainer profile." />
}
