---
phase: 19-deployment-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - next.config.ts
  - .env.example
  - vercel.json
autonomous: false
requirements:
  - DEPLOY-01
  - DEPLOY-02
must_haves:
  truths:
    - "CSP header in next.config.ts includes all Clerk CDN domains so Clerk sign-in popup and scripts load without CSP violations"
    - "vercel.json Cross-Origin-Opener-Policy is same-origin-allow-popups so Clerk OAuth popups function"
    - ".env.example documents every environment variable needed for a production Vercel deployment including Clerk keys"
    - "draftpokemon.com loads over HTTPS with working Clerk auth (user-verified)"
  artifacts:
    - path: "next.config.ts"
      provides: "CSP header with Clerk domains in script-src, connect-src, img-src, frame-src"
      contains: "clerk"
    - path: ".env.example"
      provides: "Complete production env var template with Clerk, Supabase, Sentry, PostHog, Upstash sections"
      contains: "NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY"
    - path: "vercel.json"
      provides: "COOP header for OAuth popups, no duplicate security headers"
      contains: "same-origin-allow-popups"
  key_links:
    - from: "next.config.ts"
      to: "Clerk CDN"
      via: "CSP connect-src, script-src, img-src, frame-src directives"
      pattern: "clerk"
    - from: ".env.example"
      to: "Vercel dashboard"
      via: "Documents all env vars that must be set in Vercel project settings"
      pattern: "CLERK_SECRET_KEY"
---

<objective>
Fix CSP headers for Clerk auth in production and prepare all configuration files for Vercel deployment to draftpokemon.com.

Purpose: Clerk auth (Discord/Google/Twitch sign-in) will be blocked by CSP in production unless the headers explicitly allow Clerk's CDN domains. The .env.example must document all required production variables so the user can configure Vercel correctly.

Output: Updated next.config.ts with Clerk-aware CSP, updated .env.example with complete production variable template, verified vercel.json configuration.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@next.config.ts
@vercel.json
@.env.example
@src/middleware.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Clerk CDN domains to CSP headers in next.config.ts</name>
  <files>next.config.ts</files>
  <read_first>next.config.ts</read_first>
  <action>
Update the Content-Security-Policy header value in the `headers()` function of next.config.ts. The current CSP on line 125 is missing all Clerk domains, which will cause auth popups and scripts to be blocked in production.

Add these Clerk domains to the existing CSP directives (do NOT remove any existing allowed origins):

**script-src**: Add `https://*.clerk.accounts.dev` and `https://challenges.cloudflare.com` (Clerk uses Cloudflare Turnstile for bot detection). Keep existing `'self' 'unsafe-eval'`.

**style-src**: Keep existing `'self' 'unsafe-inline'`.

**img-src**: Add `https://img.clerk.com` and `https://img.clerkstatic.com` (Clerk user avatars and branding). Keep all existing sources.

**connect-src**: Add `https://*.clerk.accounts.dev` and `https://*.clerk.dev` and `https://clerk.draftpokemon.com` and `https://api.clerk.com` (Clerk API calls from the browser). Keep all existing sources.

**frame-src**: Add `https://*.clerk.accounts.dev` and `https://challenges.cloudflare.com` (Clerk sign-in modal iframes). Keep existing `'self' https://accounts.google.com https://discord.com`.

**worker-src**: Add `'self' blob:` directive (Clerk uses web workers).

For readability, break the CSP string into a template literal or concatenated string with one directive per line, then join with semicolons. This makes future CSP changes easier to review.

IMPORTANT: CSP must remain ONLY in next.config.ts, NOT in vercel.json (per STATE.md constraint -- conflict risk if both define CSP).

Also add `https://img.clerk.com` to the Next.js `images.remotePatterns` array so `next/image` can optimize Clerk avatars:
```
{
  protocol: 'https',
  hostname: 'img.clerk.com',
}
```
  </action>
  <verify>
    <automated>cd "C:/Users/msidh/Documents/Projects/Pokemon Draft" && npx next build 2>&1 | head -30 && grep -c "clerk" next.config.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep "clerk.accounts.dev" next.config.ts` returns matches in script-src, connect-src, and frame-src
    - `grep "img.clerk.com" next.config.ts` returns matches in img-src and images.remotePatterns
    - `grep "api.clerk.com" next.config.ts` returns a match in connect-src
    - `grep "challenges.cloudflare.com" next.config.ts` returns matches in script-src and frame-src
    - `grep "worker-src" next.config.ts` returns a match with `'self' blob:`
    - `npm run build` completes without errors (type-checks next.config.ts)
    - No CSP directives exist in vercel.json (only COOP/COEP remain)
  </acceptance_criteria>
  <done>CSP header includes all Clerk CDN domains across script-src, connect-src, img-src, frame-src, and worker-src. Build passes. No CSP in vercel.json.</done>
</task>

<task type="auto">
  <name>Task 2: Update .env.example with complete production variable template</name>
  <files>.env.example</files>
  <read_first>.env.example</read_first>
  <action>
Rewrite .env.example to document every environment variable needed for production deployment on Vercel. The current file is outdated -- it references NextAuth (removed) and Supabase Auth (replaced by Clerk), and is missing Clerk variables entirely.

Structure the file with these sections and variables:

**SUPABASE (REQUIRED):**
- `NEXT_PUBLIC_SUPABASE_URL` -- Supabase project URL
- `NEXT_PUBLIC_SUPABASE_ANON_KEY` -- Supabase anon key (client-safe, RLS-gated)
- `SUPABASE_SERVICE_ROLE_KEY` -- Server-only, bypasses RLS (add WARNING comment: never expose client-side)

**CLERK AUTHENTICATION (REQUIRED):**
- `NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY` -- Clerk publishable key (pk_test_* for dev, pk_live_* for prod)
- `CLERK_SECRET_KEY` -- Clerk secret key (sk_test_* for dev, sk_live_* for prod)
- `NEXT_PUBLIC_CLERK_SIGN_IN_URL=/sign-in`
- `NEXT_PUBLIC_CLERK_SIGN_UP_URL=/sign-up`
- Add comment: "Production keys (pk_live_*, sk_live_*) must be set in Vercel Production environment scope, NOT preview scope"

**SITE URL (REQUIRED for production):**
- `NEXT_PUBLIC_SITE_URL` -- e.g., `https://draftpokemon.com`

**SENTRY ERROR TRACKING (Phase 20):**
- `NEXT_PUBLIC_SENTRY_DSN`
- `SENTRY_ORG`
- `SENTRY_PROJECT`
- `SENTRY_AUTH_TOKEN`

**POSTHOG ANALYTICS (Phase 20):**
- `NEXT_PUBLIC_POSTHOG_KEY`
- `NEXT_PUBLIC_POSTHOG_HOST=https://us.i.posthog.com`

**RATE LIMITING -- Upstash Redis (RECOMMENDED):**
- `UPSTASH_REDIS_REST_URL`
- `UPSTASH_REDIS_REST_TOKEN`

**DISCORD WEBHOOK (for feedback):**
- `NEXT_PUBLIC_DISCORD_WEBHOOK_URL` -- Discord webhook for /feedback page submissions

**PWA / PUSH NOTIFICATIONS (OPTIONAL):**
- `NEXT_PUBLIC_VAPID_PUBLIC_KEY`
- `VAPID_PRIVATE_KEY`

Remove the old NextAuth section entirely. Remove old Supabase auth references (OAuth provider setup in Supabase dashboard). Update the PRODUCTION DEPLOYMENT CHECKLIST at the bottom to reflect the current Clerk-based auth stack:

Production Deployment Checklist:
1. VERCEL PROJECT SETUP:
   - [ ] Import repo to Vercel
   - [ ] Add custom domain: draftpokemon.com
   - [ ] Configure DNS (CNAME or A record per Vercel docs)
   - [ ] Wait for SSL certificate provisioning
   - [ ] Verify HTTPS works

2. VERCEL ENVIRONMENT VARIABLES (Production scope):
   - [ ] NEXT_PUBLIC_SUPABASE_URL
   - [ ] NEXT_PUBLIC_SUPABASE_ANON_KEY
   - [ ] NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY (pk_live_*)
   - [ ] CLERK_SECRET_KEY (sk_live_*)
   - [ ] NEXT_PUBLIC_CLERK_SIGN_IN_URL=/sign-in
   - [ ] NEXT_PUBLIC_CLERK_SIGN_UP_URL=/sign-up
   - [ ] NEXT_PUBLIC_SITE_URL=https://draftpokemon.com

3. CLERK DASHBOARD (Production instance):
   - [ ] Create production instance (or switch from dev)
   - [ ] Enable Discord OAuth provider -- set callback to https://draftpokemon.com
   - [ ] Enable Google OAuth provider -- set callback to https://draftpokemon.com
   - [ ] Enable Twitch OAuth provider -- set callback to https://draftpokemon.com
   - [ ] Add draftpokemon.com to allowed origins

4. DNS CONFIGURATION:
   - [ ] Add CNAME record pointing to cname.vercel-dns.com (or A record to 76.76.21.21)
   - [ ] Wait for propagation (up to 48h, typically 15min-2h)
   - [ ] Verify with: dig draftpokemon.com

5. POST-DEPLOY VERIFICATION:
   - [ ] Visit https://draftpokemon.com -- landing page loads
   - [ ] Sign in with Discord -- no CSP errors in console
   - [ ] Sign in with Google -- no CSP errors in console
   - [ ] Create a draft -- Supabase connection works
   - [ ] /api/health returns healthy
   - [ ] Open browser DevTools Console -- no CSP violation warnings
  </action>
  <verify>
    <automated>cd "C:/Users/msidh/Documents/Projects/Pokemon Draft" && grep -c "CLERK" .env.example && grep -c "NextAuth" .env.example | grep "0" && grep "pk_live_" .env.example</automated>
  </verify>
  <acceptance_criteria>
    - `grep "NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY" .env.example` returns a match
    - `grep "CLERK_SECRET_KEY" .env.example` returns a match
    - `grep "pk_live_" .env.example` returns a match (production key reminder)
    - `grep "NextAuth" .env.example` returns NO matches (removed)
    - `grep "NEXT_PUBLIC_DISCORD_WEBHOOK_URL" .env.example` returns a match
    - `grep "draftpokemon.com" .env.example` returns matches in checklist
    - `grep "SENTRY_AUTH_TOKEN" .env.example` returns a match
    - File contains "PRODUCTION DEPLOYMENT CHECKLIST" section
  </acceptance_criteria>
  <done>.env.example documents all production env vars for Clerk-based auth stack. NextAuth references removed. Deployment checklist covers Vercel, Clerk dashboard, DNS, and post-deploy verification steps.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Deploy to Vercel and verify Clerk auth on draftpokemon.com</name>
  <files>next.config.ts, .env.example, vercel.json</files>
  <read_first>.env.example</read_first>
  <action>
This is a human verification checkpoint. Claude has completed the code changes (CSP headers and env template). The user now needs to:

1. Deploy to Vercel (import repo, set env vars, add custom domain)
2. Configure Clerk production instance (OAuth callbacks for draftpokemon.com)
3. Set up DNS (CNAME to cname.vercel-dns.com)
4. Verify auth works without CSP errors

Refer to the deployment checklist in .env.example for detailed steps.
  </action>
  <what-built>
    CSP headers updated in next.config.ts to allow all Clerk CDN domains (scripts, API calls, images, iframes, workers). Production environment template updated in .env.example with complete Clerk configuration and deployment checklist.
  </what-built>
  <how-to-verify>
    Follow the deployment checklist in .env.example:

    1. **Vercel Setup:**
       - Go to vercel.com/new and import the Pokemon Draft repo
       - Add custom domain: draftpokemon.com in Settings > Domains
       - Set ALL environment variables listed in .env.example in the Production environment scope
       - CRITICAL: Use Clerk production keys (pk_live_*, sk_live_*), NOT test keys

    2. **Clerk Dashboard:**
       - In your Clerk production instance, register OAuth callbacks for draftpokemon.com
       - Add draftpokemon.com to allowed origins
       - Enable Discord, Google, and Twitch as social connections

    3. **DNS Configuration:**
       - Add CNAME record for draftpokemon.com pointing to cname.vercel-dns.com
       - Wait for propagation (check with `dig draftpokemon.com` or nslookup)

    4. **Verification (once DNS propagates):**
       - Visit https://draftpokemon.com -- page loads with no errors
       - Open browser DevTools Console tab
       - Click Sign In and authenticate with Discord
       - Check Console -- there should be ZERO CSP violation errors
       - Repeat sign-in test with Google
       - Visit https://draftpokemon.com/api/health -- should return healthy
  </how-to-verify>
  <verify>
    <automated>echo "Human verification checkpoint -- user confirms deployment works"</automated>
  </verify>
  <done>draftpokemon.com is live with HTTPS, Clerk auth (Discord/Google) works without CSP errors, DNS resolves correctly.</done>
  <resume-signal>Type "approved" if auth works without CSP errors on draftpokemon.com, or describe any issues you see (CSP errors, auth failures, DNS problems)</resume-signal>
</task>

</tasks>

<verification>
Phase 19 is complete when:
1. CSP headers in next.config.ts include Clerk domains (grep-verifiable)
2. .env.example is updated with Clerk vars and production checklist
3. draftpokemon.com is live with HTTPS and working Clerk auth (user-verified)
4. No CSP violation errors in browser console on draftpokemon.com
</verification>

<success_criteria>
- draftpokemon.com loads over HTTPS
- Discord and Google sign-in work on draftpokemon.com without CSP console errors
- Clerk OAuth callbacks are registered against draftpokemon.com
- Build passes with updated next.config.ts
</success_criteria>

<output>
After completion, create `.planning/phases/19-deployment-foundation/19-01-SUMMARY.md`
</output>
