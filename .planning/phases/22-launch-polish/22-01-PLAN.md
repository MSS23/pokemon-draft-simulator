---
phase: 22-launch-polish
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/page.tsx
autonomous: true
requirements:
  - LAND-01
  - LAND-02
  - LAND-03
  - LAND-05

must_haves:
  truths:
    - "A VGC player landing on draftpokemon.com understands the value within 5 seconds"
    - "Hero says 'draft leagues for competitive Pokemon' with CTAs to /create-draft and /join-draft"
    - "How it works section shows Create > Draft > Play in 3 visual steps"
    - "Copy mentions VGC and draft leagues but does not exclude singles players"
  artifacts:
    - path: "src/app/page.tsx"
      provides: "Redesigned landing page with VGC hero, how-it-works, CTAs"
      contains: "VGC"
  key_links:
    - from: "src/app/page.tsx"
      to: "/create-draft"
      via: "CTA button onClick"
      pattern: "router.push.*create-draft"
    - from: "src/app/page.tsx"
      to: "/join-draft"
      via: "CTA button onClick"
      pattern: "router.push.*join-draft"
---

<objective>
Sharpen the landing page for VGC competitive Pokemon community launch. The current page has good structure but generic messaging. Refine the hero copy, how-it-works section, and CTAs to speak directly to competitive Pokemon players arriving from Reddit, Twitter/X, and Discord.

Purpose: When a VGC player hits draftpokemon.com from a social link, they must understand "this is a draft league tool for my community" within 5 seconds and see clear CTAs.

Output: Updated src/app/page.tsx with VGC-targeted messaging.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/22-launch-polish/22-CONTEXT.md
@src/app/page.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Sharpen hero copy and 5-second value proposition (LAND-01, LAND-05)</name>
  <files>src/app/page.tsx</files>
  <read_first>src/app/page.tsx</read_first>
  <action>
Modify the hero section of src/app/page.tsx. Keep the existing component structure, animations, and imports intact. Change ONLY the text content and the tag line.

1. **Tag line** (the small pill at top): Change from "The #1 Pokemon Draft Platform" to:
   ```
   Built for VGC & Draft League Communities
   ```

2. **Headline h1**: Change from "Where Pokemon Champions draft." to:
   ```
   Draft leagues for
   competitive Pokemon.
   ```
   Keep the gradient span on "competitive Pokemon" (replacing "Champions").

3. **Subtitle paragraph**: Change from the current text to:
   ```
   The free platform your VGC community actually needs. Run snake drafts, auctions, and full league seasons — no spreadsheets, no signup walls.
   ```

4. **Primary CTA button text**: Change "Create Draft" to "Start a Draft". Keep the Plus and ArrowRight icons.

5. **Secondary CTA button text**: Keep "Join a Draft" as-is. It already matches the requirement.

Do NOT change: animation variants, layout classes, SidebarLayout wrapper, auth redirect logic, sign-in link, background elements (pokeball SVG, glows, scan lines).
  </action>
  <verify>
    <automated>grep -c "VGC" src/app/page.tsx | grep -q "[1-9]" && grep -c "competitive Pokemon" src/app/page.tsx | grep -q "[1-9]" && grep -c "Start a Draft" src/app/page.tsx | grep -q "[1-9]" && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - src/app/page.tsx contains "VGC" at least once
    - src/app/page.tsx contains "competitive Pokemon" in the hero
    - Primary CTA reads "Start a Draft"
    - Subtitle mentions "VGC community"
    - Tag line reads "Built for VGC & Draft League Communities"
  </acceptance_criteria>
  <done>Hero section communicates VGC draft league value within 5 seconds. CTA says "Start a Draft" linking to /create-draft.</done>
</task>

<task type="auto">
  <name>Task 2: Refine How-It-Works and features copy for VGC audience (LAND-02, LAND-03)</name>
  <files>src/app/page.tsx</files>
  <read_first>src/app/page.tsx</read_first>
  <action>
Modify the content arrays and section copy in src/app/page.tsx. Keep all component structure, animations, and styling unchanged.

1. **Top 3-step cards** (the inline array near line 199): Update to a clearer Create > Draft > Play flow:
   - Card 1: icon Plus, title "Create a Draft", desc "Pick VGC Reg H, Smogon, or a custom format. Set budget, team count, and share a 6-character room code."
   - Card 2: icon Swords, title "Draft Your Squad", desc "Take turns picking Pokemon in real-time. Snake or auction — with wishlist, auto-pick, and budget tracking built in."
   - Card 3: icon Trophy, title "Run Your League", desc "Full season with standings, matchups, trades, and playoff brackets. Track KOs per game and crown a champion."

2. **FEATURES array** (around line 50): Update descriptions to be more VGC-specific:
   - "Real-time drafting" desc: "Every pick syncs instantly. No refreshing, no lag — WebSocket-powered so your 8-player VGC draft runs smooth."
   - "Community-driven formats" desc: "VGC Regulation H, Smogon OU, National Dex, and custom rulesets. Auto-enforced ban lists and cost tiers so nobody sneaks in a restricted legendary."
   - "Full league system" desc: "Standings, round-robin fixtures, trades with commissioner approval, and playoff brackets. Everything a competitive draft league needs."

3. **STEPS array** (around line 44): Update:
   - Step 01: title "Pick your format", desc "VGC Reg H, Smogon tiers, or fully custom — choose a template or build your own ruleset with custom bans and costs."
   - Step 02: title "Share the room code", desc "Send a 6-character code to your group. No account needed to join as a guest."
   - Step 03: title "Draft in real-time", desc "Snake or auction with turn timer, wishlist queue, and auto-pick. Budget tracking keeps it competitive."

4. **Section header** "Built for Pokemon Champions" (line ~247): Change to "Built for competitive Pokemon"

5. **Final CTA section**: Change "Join the champions" to "Your next draft league starts here". Change subtitle to:
   ```
   Create your first draft in under a minute. Free, no signup required, any format your community plays.
   ```

Do NOT change: Tailwind classes, motion variants, cardHover, refs, imports, TRUST_PROPS array (those are fine as-is).
  </action>
  <verify>
    <automated>grep -c "Reg H" src/app/page.tsx | grep -q "[1-9]" && grep -c "Your next draft league" src/app/page.tsx | grep -q "[1-9]" && grep -c "Built for competitive" src/app/page.tsx | grep -q "[1-9]" && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - How-it-works cards mention VGC Reg H
    - Features mention VGC-specific details (8-player, restricted legendary)
    - Final CTA reads "Your next draft league starts here"
    - Section header reads "Built for competitive Pokemon"
    - Copy mentions both VGC and Smogon (inclusive of singles players)
  </acceptance_criteria>
  <done>All landing page sections speak to VGC draft league players while remaining inclusive of Smogon/singles. How-it-works shows Create > Draft > Play flow.</done>
</task>

</tasks>

<verification>
1. `npm run build` completes without errors
2. Landing page renders at localhost:3000 with updated copy
3. grep confirms VGC, competitive Pokemon, Reg H, Start a Draft all present
</verification>

<success_criteria>
- Hero communicates "draft leagues for competitive Pokemon" value in under 5 seconds
- CTAs link to /create-draft and /join-draft
- How-it-works section shows Create > Draft > Play (3 steps)
- Copy mentions VGC and draft leagues but also references Smogon (inclusive)
- Build passes with no type errors
</success_criteria>

<output>
After completion, create `.planning/phases/22-launch-polish/22-01-SUMMARY.md`
</output>
